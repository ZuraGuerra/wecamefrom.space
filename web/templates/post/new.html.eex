<h2>New post</h2>

<%= render "form.html", changeset: @changeset,
                        action: post_path(@conn, :create),
                        conn: @conn %>

<%= link "Back", to: post_path(@conn, :index) %>
